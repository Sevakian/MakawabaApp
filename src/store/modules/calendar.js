import dataContext from './calendar/dataContext'
import dataView from './calendar/dataView'
import tables from './calendar/tables'
import verwContext from './calendar/verwContext'

export default {
    namespaced: true,
    
    state: {
    },
    getters: {
    },
    mutations: {
    },
    actions: {
    },
    modules: {
      dataContext, 
      dataView,
      tables,
      verwContext
    }
  }
  
